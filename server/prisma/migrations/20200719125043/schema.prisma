generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url = "***"
}

model User {
  email    String @unique
  id       Int    @default(autoincrement()) @id
  password String
}
